
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-gray-50">
    {{#if user}}
      {{#if (eq user.role 'admin')}}
        {{> navbar}}
      {{else}}
        {{> customer_navbar}}
      {{/if}}
    {{else}}
      <header class="bg-white shadow-sm border-b border-gray-200">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4">
            <div class="flex justify-between items-center">
                <div>
                    <h1 class="text-2xl font-bold text-gray-900">🛒 Marketplace</h1>
                    <p class="text-sm text-gray-500">Discover amazing products from trusted sellers</p>
                </div>
                <div class="flex items-center space-x-4">
                    <a href="/auth/login" class="text-sm text-gray-700 hover:text-gray-900 font-medium">
                        Login
                    </a>
                    <a href="/auth/register" class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-md text-sm font-medium">
                        Register
                    </a>
                </div>
            </div>
        </div>
      </header>
    {{/if}}

    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <div class="bg-gradient-to-r from-blue-600 to-blue-800 rounded-lg shadow-lg p-8 mb-8 text-white">
            <h2 class="text-3xl font-bold mb-2">Welcome to Our Marketplace</h2>
            <p class="text-blue-100">Browse through our collection of quality products from verified sellers</p>
        </div>

        <div class="grid grid-cols-1 sm:grid-cols-3 gap-4 mb-8">
            <div class="bg-white rounded-lg shadow p-6 text-center">
                <div class="text-3xl font-bold text-blue-600">{{products.length}}</div>
                <div class="text-sm text-gray-500">Products Available</div>
            </div>
            <div class="bg-white rounded-lg shadow p-6 text-center">
                <div class="text-3xl font-bold text-green-600">100%</div>
                <div class="text-sm text-gray-500">Verified Sellers</div>
            </div>
            <div class="bg-white rounded-lg shadow p-6 text-center">
                <div class="text-3xl font-bold text-purple-600">24/7</div>
                <div class="text-sm text-gray-500">Customer Support</div>
            </div>
        </div>

        <div class="mb-6">
            <h3 class="text-xl font-bold text-gray-900 mb-4">All Products</h3>
        </div>

        {{#if products.length}}
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
            {{#each products}}
            <a href="/admin/products/{{this.id}}" class="bg-white rounded-lg shadow hover:shadow-xl transition-shadow duration-300 overflow-hidden group">
                <div class="h-48 bg-gray-200 overflow-hidden">
                    {{#if this.imageUrl}}
                    <img src="{{this.imageUrl}}" alt="{{this.name}}" class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300">
                    {{else}}
                    <div class="w-full h-full flex items-center justify-center bg-gradient-to-br from-gray-100 to-gray-200">
                        <svg class="w-16 h-16 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4" />
                        </svg>
                    </div>
                    {{/if}}
                </div>

                <div class="p-4">
                    <div class="mb-2">
                        <span class="inline-block px-2 py-1 text-xs font-semibold rounded-full bg-blue-100 text-blue-800">
                            {{this.category}}
                        </span>
                    </div>

                    <h4 class="text-lg font-semibold text-gray-900 mb-2 line-clamp-2 group-hover:text-blue-600 transition-colors">
                        {{this.name}}
                    </h4>

                    <p class="text-sm text-gray-600 mb-3 line-clamp-2">
                        {{this.description}}
                    </p>

                    <div class="flex justify-between items-center mb-3">
                        <div class="text-xl font-bold text-gray-900">
                            {{formatPrice this.price}}
                        </div>
                        <div class="text-sm text-gray-500">
                            Stock: {{this.stock}}
                        </div>
                    </div>

                    <div class="pt-3 border-t border-gray-100">
                        <div class="flex items-center text-sm text-gray-600">
                            <svg class="w-4 h-4 mr-1 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                            </svg>
                            <span class="truncate">{{this.seller.name}}</span>
                        </div>
                    </div>

                    <div class="mt-4">
                        <span class="inline-block w-full text-center bg-blue-600 hover:bg-blue-700 text-white font-medium py-2 px-4 rounded-md transition-colors">
                            View Details
                        </span>
                    </div>
                </div>
            </a>
            {{/each}}
        </div>
        {{else}}
        <div class="bg-white rounded-lg shadow p-12 text-center">
            <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 13V6a2 2 0 00-2-2H6a2 2 0 00-2 2v7m16 0v5a2 2 0 01-2 2H6a2 2 0 01-2-2v-5m16 0h-2.586a1 1 0 00-.707.293l-2.414 2.414a1 1 0 01-.707.293h-3.172a1 1 0 01-.707-.293l-2.414-2.414A1 1 0 006.586 13H4" />
            </svg>
            <h3 class="mt-2 text-lg font-medium text-gray-900">No products yet</h3>
            <p class="mt-1 text-sm text-gray-500">Check back later for amazing products!</p>
        </div>
        {{/if}}
    </main>

    <footer class="bg-white border-t border-gray-200 mt-12">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
            <div class="text-center text-sm text-gray-500">
                <p>© Arfan Nur Ivandi - Challenge Fullstack. Magang DOT Malang 2026 Batch 1.</p>
            </div>
        </div>
    </footer>

    <style>
        .line-clamp-2 {
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }
    </style>
</body>
</html>
                